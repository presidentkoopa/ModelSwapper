# Build artifact -- rebuild with ./build.ps1
ModelSwapper.pk3

# Windows junk
Thumbs.db
desktop.ini
*.lnk

# Build artifacts -- rebuild with ./build.ps1
ModelSwapper_*.pk3
ModelSwapper-REMAP.pk3
ModelSwapper-QUEST.pk3

# Packaged release archives. Built from the tree, never a source of truth --
# and at ~78MB one of these in history would outweigh the entire repo.
*.zip

# Generator scratch output -- rebuild with ./tools_gen_bd21.ps1
.gen/

# Engine log, written into cwd when the engine is run from here
questzdoom.log
*.log
doomxr-log.txt

# engine load-test artifact, written into the repo root by doomxr.exe
log-debug.txt

# Python tool scratch
__pycache__/
